# Generated by scap2salt — renders in the MLM Web UI 'Formulas' tab.
pci_dss:
  $type: group
  $name: PCI-DSS v4 Hardening
  $help: 'Native Salt enforcement generated from xccdf_org.ssgproject.content_profile_pci-dss-4 (MAC: apparmor). Untick a category to skip it.'
  enabled:
    $type: boolean
    $default: True
    $name: Enable PCI-DSS hardening
  sysctl:
    $type: boolean
    $default: True
    $name: "Kernel parameters (sysctl)"
  packages:
    $type: boolean
    $default: True
    $name: "Package install / removal"
  services:
    $type: boolean
    $default: True
    $name: "Service enable / disable"
  permissions:
    $type: boolean
    $default: True
    $name: "File permissions & ownership"
  kernel_modules:
    $type: boolean
    $default: True
    $name: "Disabled kernel modules"
  sshd:
    $type: boolean
    $default: True
    $name: "SSH server hardening"
  lineinfile:
    $type: boolean
    $default: True
    $name: "Config-file settings (login.defs, securetty, etc.)"
  pam:
    $type: boolean
    $default: True
    $name: "PAM module arguments (pwquality, pam_unix, pam_wheel)"
  sudo:
    $type: boolean
    $default: True
    $name: "sudo Defaults (sudoers.d drop-ins)"
  audit:
    $type: boolean
    $default: True
    $name: "Audit (auditd rules + auditd.conf)"
  dconf:
    $type: boolean
    $default: True
    $name: "GNOME desktop (dconf) policy"
  coredump:
    $type: boolean
    $default: True
    $name: "systemd core dump policy"
  grub:
    $type: boolean
    $default: True
    $name: "GRUB kernel command-line arguments"
  firewall:
    $type: boolean
    $default: True
    $name: "Firewall (iptables loopback rules)"
  aide:
    $type: boolean
    $default: True
    $name: "File integrity (AIDE)"
  rpm:
    $type: boolean
    $default: True
    $name: "Package signatures & verification (RPM/GPG)"
  mac:
    $type: boolean
    $default: True
    $name: "Mandatory Access Control (SELinux / AppArmor)"
